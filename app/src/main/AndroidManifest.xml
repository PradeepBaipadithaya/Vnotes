<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vnotes">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VNotes">
        <activity
            android:name=".no_notes"
            android:exported="false" />
        <activity
            android:name=".no_internet"
            android:exported="false" />
        <activity
            android:name=".second_page"
            android:exported="false"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".third_page"
            android:exported="false"
            android:parentActivityName=".second_page" />
        <activity
            android:name=".fourth_page"
            android:exported="false"
            android:parentActivityName=".third_page" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>